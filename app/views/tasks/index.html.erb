<h1>タスク一覧</h1>

<ur>
  <% @tasks.each do |task| %>
  <li><%= link_to task.id, task %> : <%= task.title %> &gt; <%= task.content %></li>
  <% end %>
</ur>

<p><%= link_to '新規タスク作成', new_task_path %></p>